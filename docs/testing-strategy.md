# Testing Strategy

The project will add tests incrementally:

- Unit tests for domain rules.
- API and integration tests for workflows.
- Architecture tests for module boundaries.
- Contract tests where useful.
- Testcontainers for external dependencies.
