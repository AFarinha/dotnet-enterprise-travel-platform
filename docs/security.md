# Security

Planned baseline:

- JWT authentication.
- Roles: `User`, `TravelAgent`, `Admin`.
- Admin-only operational endpoints.
- Ownership checks for normal users.
- No sensitive data in logs.
- Input validation on every endpoint.
